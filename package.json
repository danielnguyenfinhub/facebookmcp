{
  "name": "finhub-facebook-mcp",
  "version": "3.0.0",
  "description": "Facebook, Instagram & WhatsApp MCP Server for Claude — FINHUB-CLAUDE-MCP",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --format=cjs --outfile=dist/index.js",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.0.0",
    "esbuild": "^0.21.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
